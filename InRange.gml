/// @desc Checks if a number is within a range.
/// @param num
/// @param min
/// @param max

var _num = argument[0];
var _min = argument[1];
var _max = argument[2];

return ((_num > _min) and (_num < _max))
